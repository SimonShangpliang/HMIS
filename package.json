{
  "name": "hmis",
  "version": "1.0.0",
  "description": "---",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm install && npm install --prefix backend && npm run start --prefix backend"
  },
  "author": "",
  "license": "ISC"
}
